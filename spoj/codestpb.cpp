#include<stdio.h>
#include<algorithm>

bool compare(int i,int j)
{
    return i<j;
}
int main()
{
    int t,n,a[100000],b[100000];
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&n);
        for(i=0;i<n;i++)
            {scanf("%d",&temp);
            a[i]=temp;
            b[i]=temp;}
        std::sort(a,a+n,compare);
        for(i=0;i<n;i++)
        {

        }

    }
}
